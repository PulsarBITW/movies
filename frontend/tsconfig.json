{
  "compilerOptions": {
    "outDir": "./dist/",
    "noImplicitAny": true,
    "module": "es6",
    "target": "es5",
    "jsx": "react-jsx", // если просто react, то import React - обязательное условие для каждого файла, если react-jsx - то импорт не нужен
    "allowJs": true,
    "moduleResolution": "node",
    "allowSyntheticDefaultImports": true, // для обработки импортов es6 так же позволяет избавиться от * as path/htmlplugin
    "esModuleInterop": true // для обработки импортов es6 так же позволяет избавиться от * as path/htmlplugin
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS" // для обработки импортов es6 так же позволяет избавиться от * as path/htmlplugin
    }
  }
}
